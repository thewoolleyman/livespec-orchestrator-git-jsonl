# Per-target matrix CI for livespec-orchestrator-git-jsonl.
#
# Generated from livespec/templates/impl-plugin/.github/workflows/
# ci.yml.jinja at copier-copy time; re-sync via `copier update --vcs-ref=master` when
# livespec publishes a new release.
#
# Authority: livespec/SPECIFICATION/contracts.md
#   §"Pre-commit step ordering" (c)-(d) — zero-py subsetting:
#   master pushes + merge_group events always run the full aggregate
#   (merge-queue safety net); pull_request events skip Python-code
#   checks when the changeset contains zero .py files.
#
# Authority: livespec/SPECIFICATION/non-functional-requirements.md
#   §"Toolchain pins" — direct tool invocations (ruff, pyright,
#   pytest, python3) in `run:` lines are forbidden; every step
#   delegates to `just <target>`.
#
# Starter scaffold: the Python-code matrix carries only the tool-
# backed checks (lint, format, types, coverage). The metadata matrix
# is empty. As livespec-orchestrator-git-jsonl populates dev-tooling/checks/
# scripts, add the corresponding `check-<slug>` targets to the
# appropriate matrix and to branch protection's required-status-
# checks list.
#
# The check matrices + check-doctor-static run in the SAME baked image the
# Fabro sandbox uses, on the local self-hosted runner — so "green in CI" and
# "green in sandbox" are the same environment (livespec
# plan/fabro-ci-image-factoring). runs-on resolves from the repo variable
# CI_RUNNER_LABELS, defaulting to the self-hosted label; set it to
# '["ubuntu-latest"]' to reroute to GitHub-hosted when the local host is down
# (the merge-gate fallback). setup, export-telemetry and ci-green stay
# ubuntu-latest — the gate must stay reportable host-down and the telemetry
# secret must never reach the runner.

name: CI

# uv occasionally fails when the PyPI file CDN (files.pythonhosted.org)
# drops a wheel download mid-stream ("broken pipe"). UV_HTTP_RETRIES
# makes uv retry the fetch in-process instead of failing the job.
# Combined with the ~/.cache/uv actions/cache step (keyed on uv.lock)
# every uv-sync job uses below, an unchanged lock installs from cache
# and never contacts PyPI at all.
env:
  UV_HTTP_RETRIES: "5"
  # Test-parallelism lane signal read by the justfile's `test_nprocs`
  # (plan/fabro-ci-image-factoring cont.5). RUNNER_ENVIRONMENT does NOT
  # propagate into container jobs, so derive the lane from the routing
  # variable here: GitHub-hosted (CI_RUNNER_LABELS flipped to an ubuntu
  # label) → `hosted` → pytest -n auto; the self-hosted/local lane → `local`
  # → -n 25%-of-cores (overridable per host via LIVESPEC_TEST_PARALLELISM).
  LIVESPEC_CI_LANE: ${{ contains(vars.CI_RUNNER_LABELS, 'ubuntu') && 'hosted' || 'local' }}

# merge_group is deliberately ABSENT: no merge queue is configured for this
# repo (verified live — zero rulesets, required_merge_queue: null; branch
# protection requires only ci-green). Keeping the vestigial trigger would be
# exactly the routing hole the trusted-event audit forbids, since merge_group
# is not a fork-gated event and the matrices below are self-hosted. Re-add it
# WITH hosted routing only if a merge queue is adopted.
on:
  pull_request:
  push:
    branches: [master]

jobs:
  setup:
    name: detect-py-changes
    runs-on: ubuntu-latest
    outputs:
      py_changed: ${{ steps.detect.outputs.py_changed }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Need the base ref reachable for the PR diff below.
          fetch-depth: 0

      - name: Detect Python file changes
        id: detect
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.base_ref }}
        run: |
          set -uo pipefail
          if [[ "$EVENT_NAME" == "pull_request" ]]; then
            # Full-depth fetch (checkout already set fetch-depth: 0) — a
            # shallow `--depth=1` fetch doesn't bring in commits prior to
            # the base ref tip, so `git diff origin/${BASE_REF}...HEAD`
            # would intermittently fail with "no merge base" when the
            # local clone's history didn't reach the divergence point.
            # Keep `|| true` as a fallback: when fetch fails (auth,
            # network) we still try the diff against whatever
            # origin/${BASE_REF} ref the checkout already populated.
            git fetch origin "$BASE_REF" || true
            changeset=$(git diff --name-only "origin/${BASE_REF}...HEAD")
            if echo "$changeset" | grep -qE '\.py$'; then
              echo "py_changed=true" >> "$GITHUB_OUTPUT"
            else
              echo "py_changed=false" >> "$GITHUB_OUTPUT"
            fi
          else
            echo "py_changed=true" >> "$GITHUB_OUTPUT"
          fi

      # `setup` is the change-detection GATE only — it deliberately does
      # NO uv pre-warm. The check-python matrix jobs `needs: setup`, so
      # any work here sits on the critical path before the matrix can
      # start. Each matrix job restores ~/.cache/uv itself (keyed on
      # uv.lock, with the `uv-${{ runner.os }}-` prefix
      # restore-key) and runs its own `uv sync`, so on the common
      # unchanged-lock run the cache already exists and every job
      # restores it in parallel with no PyPI fetch. A serial pre-warm
      # here would only help the first run after a lock change — and the
      # prefix restore-key already bounds that to the delta wheels — so
      # it is not worth taxing every run's wall-clock.

  check-python:
    name: ${{ matrix.target }}
    needs: setup
    # Local self-hosted runner in the baked sandbox image (fallback: set the
    # CI_RUNNER_LABELS repo variable to '["ubuntu-latest"]'). The container is
    # the SAME tag the Fabro sandbox runs — that sameness collapses the
    # green-in-CI / red-in-sandbox drift this epic exists to remove.
    runs-on: ${{ fromJSON(vars.CI_RUNNER_LABELS || '["self-hosted","local-ci"]') }}
    container:
      image: ghcr.io/thewoolleyman/livespec-fabro-sandbox:python-v0.54.2
    env:
      MISE_DATA_DIR: /root/.local/share/mise
      MISE_NOT_FOUND_AUTO_INSTALL: "0"
      GITHUB_TOKEN: ${{ github.token }}
      GH_TOKEN: ${{ github.token }}
    strategy:
      fail-fast: false
      matrix:
        target:
          - check-lint
          - check-format
          - check-types
          - check-coverage
          # fleet-ci-aggregate-coverage (epic livespec-cf4bcu) slice 5:
          # canonical `just check` aggregate slugs that inspect .py source
          # (AST/style/coverage checks over the product + test trees) but
          # were not yet run in CI. The py_changed gate is correct for
          # these — nothing to check when a PR changes no .py.
          - check-private-calls
          - check-global-writes
          - check-supervisor-discipline
          - check-no-raise-outside-io
          - check-no-except-outside-io
          - check-no-fmt-directives
          - check-public-api-result-typed
          - check-main-guard
          - check-wrapper-shape
          - check-keyword-only-args
          - check-match-keyword-only
          - check-no-inheritance
          - check-assert-never-exhaustiveness
          - check-newtype-domain-primitives
          - check-all-declared
          - check-no-write-direct
          - check-pbt-coverage-pure-modules
          - check-check-mutation
          - check-file-lloc
          - check-no-lloc-soft-warnings
          - check-per-file-coverage
          - check-rop-pipeline-shape
          - check-tests-mirror-pairing
          - check-tests-no-subprocess-spawn
    steps:
      # Per-step `if:` (not job-level) so each matrix entry still
      # reports a status check named `${{ matrix.target }}`. With
      # job-level `if:`, a skipped matrix collapses to one
      # `check-python` status, which doesn't satisfy branch
      # protection's requirement that each specific check name be
      # present.
      - name: Skip when no .py changes
        if: needs.setup.outputs.py_changed != 'true'
        run: echo "::notice::skipped ${{ matrix.target }} — no .py changes in this PR"

      - name: Checkout
        if: needs.setup.outputs.py_changed == 'true'
        uses: actions/checkout@v4

      # Hosted routing checks out the workspace as uid 1001 while steps run git as
      # ROOT in the baked container → "dubious ownership" (exit 128). Write
      # safe.directory to the global gitconfig so even resolve_repo_root reads it.
      - name: Trust workspace for git (hosted container root-vs-checkout uid)
        if: needs.setup.outputs.py_changed == 'true'
        run: git config --global --add safe.directory '*'

      # Tools are baked into the image; `mise install` is a near-no-op that just
      # confirms the pinned versions. A fresh container does not TRUST the repo
      # .mise.toml, so `mise trust` is required for the shims to resolve.
      # (Replaces the mise-action install + the actions/cache uv restore — the
      # local runner's persistent ~/.cache/uv is the cache now.)
      - name: mise trust + install (baked tools — install is a near-no-op)
        if: needs.setup.outputs.py_changed == 'true'
        run: |
          mise trust
          mise install

      - name: Install Python dev deps via uv
        if: needs.setup.outputs.py_changed == 'true'
        run: uv sync --all-groups

      - name: just ${{ matrix.target }}
        if: needs.setup.outputs.py_changed == 'true'
        run: |
          # pyright (check-types) shells out to `npm`; under the container-hooks
          # HOME the mise npm shim busy-loops (no node registered for that HOME).
          # Put the image's real node/npm — derived from the shim's own location,
          # never a hardcoded image path — ahead of the shim on PATH. Harmless
          # for the non-npm targets.
          shims_dir="$(dirname "$(command -v npm)")"
          node_bin="$(cd "$shims_dir/../installs/node/latest/bin" 2>/dev/null && pwd || ls -d "$shims_dir/../installs/node"/*/bin 2>/dev/null | head -1)"
          [ -n "$node_bin" ] && export PATH="$node_bin:$PATH"
          just ${{ matrix.target }}

  # livespec core's doctor STATIC phase (reference-discipline + out-of-band
  # invariants), wired fleet-wide per livespec epic livespec-6jfq. core ships
  # the checker (doctor_static.py — self-contained, bare python3); this job
  # checks core out at the tag THIS repo pins (.livespec.jsonc compat.pinned)
  # and points the recipe at it via LIVESPEC_CORE_PLUGIN_ROOT. Its OWN job
  # (not the matrix) because it needs a second checkout — livespec core —
  # that no other target does.
  check-doctor-static:
    name: check-doctor-static
    # Self-hosted in the baked image. The livespec-core checkout below is a
    # fresh PUBLIC clone INSIDE the container (no host mount — containment
    # preserved).
    runs-on: ${{ fromJSON(vars.CI_RUNNER_LABELS || '["self-hosted","local-ci"]') }}
    container:
      image: ghcr.io/thewoolleyman/livespec-fabro-sandbox:python-v0.54.2
    env:
      MISE_DATA_DIR: /root/.local/share/mise
      MISE_NOT_FOUND_AUTO_INSTALL: "0"
      GITHUB_TOKEN: ${{ github.token }}
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Trust the workspace for git under hosted routing (uid-1001 checkout vs
      # root-in-container). See check-python for the full rationale.
      - name: Trust workspace for git (hosted container root-vs-checkout uid)
        run: git config --global --add safe.directory '*'

      - name: mise trust + install (baked tools — install is a near-no-op)
        run: |
          mise trust
          mise install

      - name: Resolve pinned livespec core ref (.livespec.jsonc compat.pinned)
        id: corepin
        run: |
          pinned="$(grep -oE '"pinned"[[:space:]]*:[[:space:]]*"[^"]*"' .livespec.jsonc | head -1 | sed -E 's/.*:[[:space:]]*"([^"]*)".*/\1/')"
          echo "ref=${pinned:-master}" >> "$GITHUB_OUTPUT"

      - name: Checkout livespec core at the pinned ref
        uses: actions/checkout@v4
        with:
          repository: thewoolleyman/livespec
          ref: ${{ steps.corepin.outputs.ref }}
          path: .livespec-core

      - name: just check-doctor-static
        run: |
          # In a container job, github.workspace (context) may be the HOST path;
          # $GITHUB_WORKSPACE (env) is the in-container mount, so derive the core
          # plugin root from it to stay container-correct.
          export LIVESPEC_CORE_PLUGIN_ROOT="${GITHUB_WORKSPACE}/.livespec-core/.claude-plugin"
          just check-doctor-static

  # Metadata matrix runs unconditionally to preserve the repo-metadata
  # safety net. check-aggregate-completeness is the wiring-completeness
  # gate (epic li-univck) — verifies the justfile `check:` aggregate
  # wires every canonical slug. check-tool-backed-check-completeness
  # (dev-tooling v0.8.0, epic li-pyright-gate) asserts the four
  # tool-backed checks (lint, format, types, coverage) are LITERAL
  # members of BOTH the `just check` targets array AND a CI matrix —
  # so the metadata matrix MUST carry it here to satisfy the both-
  # surfaces invariant it enforces.
  check-metadata:
    name: ${{ matrix.target }}
    needs: setup
    # Self-hosted in the baked image, same rationale as check-python above.
    runs-on: ${{ fromJSON(vars.CI_RUNNER_LABELS || '["self-hosted","local-ci"]') }}
    container:
      image: ghcr.io/thewoolleyman/livespec-fabro-sandbox:python-v0.54.2
    env:
      MISE_DATA_DIR: /root/.local/share/mise
      MISE_NOT_FOUND_AUTO_INSTALL: "0"
      GITHUB_TOKEN: ${{ github.token }}
      GH_TOKEN: ${{ github.token }}
    strategy:
      fail-fast: false
      matrix:
        target:
          - check-aggregate-completeness
          - check-handoff-dispatch-routing
          - check-local-memory-drift-audit
          # check-no-direct-destructive-cli greps the agent-facing trees
          # for direct destructive-default CLI invocations
          # (livespec/SPECIFICATION/non-functional-requirements.md
          # §"Destructive-default CLI wrapping") so a violation fails CI.
          - check-no-direct-destructive-cli
          - check-no-shadow-ledger-body-typechecks
          - check-plan-thread-anchor-declared
          - check-primary-checkout-commit-refuse-hook-installed
          # Byte-identity sibling of the commit-refuse check above: reads
          # the `neutral_hook_body_path` role key from pyproject.toml (a
          # config surface, no .py source), so it lives in this always-run
          # metadata matrix. This repo is an orchestrator plugin, NOT a
          # Driver, so it declares NO neutral_hook_body_path and the check
          # no-ops here — it is wired so CI provably runs the whole
          # canonical aggregate.
          - check-no-shadow-ledger-body-identical
          # check-red-green-replay (no-arg) validates the COMMIT RANGE
          # origin/master..HEAD — every non-merge commit touching product
          # impl .py must carry EITHER the TDD-Red-*/TDD-Green-* pair
          # shape OR the TDD-Suite-Green-* green-verified shape,
          # regardless of subject prefix (livespec-dev-tooling v0.11.0
          # commit-range mode). It runs unconditionally: on zero-.py PRs
          # the range carries no product .py and trivially passes; on
          # master pushes the range is empty.
          - check-red-green-replay
          - check-self-hosted-routing
          - check-source-trees-scoped-to-consumer
          - check-tool-backed-check-completeness
          # fleet-ci-aggregate-coverage (epic livespec-cf4bcu) slice 5:
          # canonical aggregate slugs that inspect NON-.py surfaces
          # (justfile, docs, marketplace/SKILL.md, AGENTS.md, mise config),
          # need full git history, or gate CI config — so they belong in
          # this always-run, fetch-depth:0 metadata matrix, NOT the
          # py-gated one. check-check-coverage-incremental diffs
          # origin/master...HEAD (needs the base ref this matrix's
          # fetch-depth:0 provides) and spawns its own pytest, so it is
          # self-contained here. The two world-gate checks
          # (check-master-ci-green, check-branch-protection-alignment) are
          # deliberately ABSENT: livespec-dev-tooling v0.37.2 excludes them
          # from the CI-mirror requirement (they enforce at pre-push under
          # an admin token; see .ai/ci-gate-discipline.md).
          - check-claude-md-coverage
          - check-heading-coverage
          - check-vendor-manifest
          - check-no-direct-tool-invocation
          - check-agents-ai-references-resolve
          - check-canonical-recipe-fidelity
          - check-check-coverage-incremental
          - check-check-tools
          - check-ci-matrix-completeness
          - check-comment-line-anchors
          - check-commit-pairs-source-and-test
          - check-fleet-marketplace-relative-sources
          - check-no-todo-registry
          - check-partition-completeness
          - check-plugin-resolution
          - check-skill-invocation-paths
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Full history for ALL matrix entries: the
          # check-red-green-replay range validation enumerates
          # origin/master..HEAD and needs origin/master plus the merge
          # base in the clone — a fetch-depth: 1 checkout would make
          # the base unresolvable and fail the check actionably (it
          # never silently passes). Cheap for this repo; harmless for
          # the other metadata entries.
          fetch-depth: 0

      # Trust the workspace for git under hosted routing (uid-1001 checkout vs
      # root-in-container). See check-python for the full rationale.
      - name: Trust workspace for git (hosted container root-vs-checkout uid)
        run: git config --global --add safe.directory '*'

      - name: mise trust + install (baked tools — install is a near-no-op)
        run: |
          mise trust
          mise install

      - name: Install Python dev deps via uv
        run: uv sync --all-groups

      # The shared livespec-dev-tooling commit-refuse-hook check enforces the
      # canonical structural `.git/hooks/pre-commit` AND `.git/hooks/pre-push`
      # body on the primary checkout per
      # livespec/SPECIFICATION/non-functional-requirements.md
      # §"Primary-checkout commit-refuse hook". `actions/checkout` clones with
      # empty hook bodies; install the canonical body by REUSING the shared
      # livespec-dev-tooling installer (NOT a vendored copy) so the invariant is
      # satisfied for the CI run. Runs AFTER `uv sync` because the installer is a
      # Python module from that package; delegates through `just` (the single
      # invocation source of truth). The installed body is STRUCTURAL (refuse
      # when `git rev-parse --git-dir` == `--git-common-dir`, armed on install)
      # and supersedes the legacy livespec.primaryPath mechanism.
      # Install BOTH facets of the Worktree-discipline concern the
      # check-primary-checkout-commit-refuse-hook-installed verifier guards:
      # the commit-refuse hooks AND the worktree-discipline pack
      # (worktree-lib.sh + branch-protection.sh). The pack scripts are
      # gitignored (installed, not tracked), so a fresh `actions/checkout`
      # clone lacks them; installing them here makes the verifier's 3rd arm
      # VALIDATE the byte-identical pack rather than SKIP it (the verifier
      # skips when neither pack script is present). Both installers are
      # from-package (NOT vendored copies) and run AFTER `uv sync` because
      # they are Python modules from that package; delegated through `just`
      # (the single invocation source of truth).
      - name: Install commit-refuse hook + worktree pack (satisfy hook invariant)
        if: matrix.target == 'check-primary-checkout-commit-refuse-hook-installed'
        run: |
          just install-commit-refuse-hooks
          just install-worktree-pack

      - name: just ${{ matrix.target }}
        env:
          # Arm the CI-matrix drift guard (fleet-ci-aggregate-coverage,
          # epic livespec-cf4bcu, slice 5): now that this repo's matrices +
          # ci-green are complete, flip check-ci-matrix-completeness from
          # warn (exit 0) to FAIL (exit 4), so any future canonical slug
          # wired into `just check` but dropped from CI reddens this leg
          # instead of silently warning. Harmless for the other metadata
          # legs — they do not read this var.
          LIVESPEC_FAIL_IF_CI_MATRIX_GAPS_EXIST: "true"
        run: just ${{ matrix.target }}

  # CLI end-to-end harness — the top-of-pyramid, user-surface tier whose
  # sole interaction surface is the `claude` CLI binary (per
  # livespec/SPECIFICATION/contracts.md §"CLI end-to-end harness
  # contract"). The harness ships from livespec-dev-tooling (v0.8.0) and
  # is consumed via the imported test_workflow_full_round_trip entry
  # point wired in tests/e2e-cli/. This job runs the MOCK tier
  # (LIVESPEC_E2E_HARNESS=mock — the one mocked boundary is the
  # `claude -p` subprocess; real install-shape setup, real structural
  # skill discovery, the real fail-closed time-bomb coverage gate, and
  # the real per-skill orchestration loop all run). The coverage gate is
  # fail-closed: a `/livespec-orchestrator-git-jsonl:*` skill added without a
  # `tests/e2e-cli/fixtures/<skill>/` directory raises CoverageGateError
  # and fails this job. The `real` tier (driving the actual `claude`
  # binary against the upstream livespec plugin paired in lockstep)
  # requires ANTHROPIC_API_KEY and is deliberately NOT run in CI.
  e2e-cli:
    name: e2e-cli
    needs: setup
    # Self-hosted in the baked image, same rationale as check-python above.
    runs-on: ${{ fromJSON(vars.CI_RUNNER_LABELS || '["self-hosted","local-ci"]') }}
    container:
      image: ghcr.io/thewoolleyman/livespec-fabro-sandbox:python-v0.54.2
    env:
      MISE_DATA_DIR: /root/.local/share/mise
      MISE_NOT_FOUND_AUTO_INSTALL: "0"
      GITHUB_TOKEN: ${{ github.token }}
      GH_TOKEN: ${{ github.token }}
      # This job's own pre-existing setting — MERGED into the single env block
      # above, never a second `env:` key. Two `env:` keys in one job is a YAML
      # duplicate-key error: GitHub rejects the whole workflow at startup, and a
      # permissive parser silently keeps only the LAST block, which would drop
      # the mock tier and turn this into a live-harness run.
      LIVESPEC_E2E_HARNESS: mock
    steps:
      - name: Skip when no .py changes
        if: needs.setup.outputs.py_changed != 'true'
        run: echo "::notice::skipped e2e-cli — no .py changes in this PR"

      - name: Checkout
        if: needs.setup.outputs.py_changed == 'true'
        uses: actions/checkout@v4

      # Trust the workspace for git under hosted routing (uid-1001 checkout vs
      # root-in-container). See check-python for the full rationale.
      - name: Trust workspace for git (hosted container root-vs-checkout uid)
        if: needs.setup.outputs.py_changed == 'true'
        run: git config --global --add safe.directory '*'

      # Tools are baked into the image; `mise install` is a near-no-op that just
      # confirms the pinned versions. A fresh container does not TRUST the repo
      # .mise.toml, so `mise trust` is required for the shims to resolve.
      # (Replaces the mise-action install + the actions/cache uv restore — the
      # local runner's persistent ~/.cache/uv is the cache now.)
      - name: mise trust + install (baked tools — install is a near-no-op)
        if: needs.setup.outputs.py_changed == 'true'
        run: |
          mise trust
          mise install

      - name: Install Python dev deps via uv
        if: needs.setup.outputs.py_changed == 'true'
        run: uv sync --all-groups

      - name: Run the CLI e2e harness (mock tier, fail-closed coverage gate)
        if: needs.setup.outputs.py_changed == 'true'
        run: just check-e2e-cli

  acceptance:
    name: acceptance
    needs: setup
    # Self-hosted in the baked image, same rationale as check-python above.
    runs-on: ${{ fromJSON(vars.CI_RUNNER_LABELS || '["self-hosted","local-ci"]') }}
    container:
      image: ghcr.io/thewoolleyman/livespec-fabro-sandbox:python-v0.54.2
    env:
      MISE_DATA_DIR: /root/.local/share/mise
      MISE_NOT_FOUND_AUTO_INSTALL: "0"
      GITHUB_TOKEN: ${{ github.token }}
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Trust the workspace for git under hosted routing (uid-1001 checkout vs
      # root-in-container). See check-python for the full rationale.
      - name: Trust workspace for git (hosted container root-vs-checkout uid)
        run: git config --global --add safe.directory '*'

      - name: mise trust + install (baked tools — install is a near-no-op)
        run: |
          mise trust
          mise install

      - name: Install Python dev deps via uv
        run: uv sync --all-groups

      - name: Run golden-master acceptance harness
        run: just acceptance

  # Closed-loop CI telemetry: export this run's per-job timings to Honeycomb
  # (dataset github-ci) and FAIL if Honeycomb does not accept them, so a broken
  # telemetry pipeline reddens master CI instead of dying silently. Master/
  # merge_group only (PRs untouched, no added latency). REQUIRES the repo secret
  # HONEYCOMB_GITHUB_CI_INGEST_KEY_LIVESPEC (the livespec-family write-only
  # ingest key). A freshly generated livespec-orchestrator-git-jsonl fails this job on its
  # first master run until that secret is added — add it, or delete this job if
  # livespec-orchestrator-git-jsonl does not send CI telemetry to Honeycomb.
  export-telemetry:
    name: export-telemetry
    needs: [setup, check-python, check-metadata, e2e-cli, acceptance]
    if: ${{ !cancelled() && github.event_name == 'push' }}
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Export CI run telemetry to Honeycomb (closed-loop verified)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          RUN_ID: ${{ github.run_id }}
          HONEYCOMB_GITHUB_CI_INGEST_KEY_LIVESPEC: ${{ secrets.HONEYCOMB_GITHUB_CI_INGEST_KEY_LIVESPEC }}
        run: bash .github/scripts/export-ci-telemetry.sh

  # fleet-ci-aggregate-coverage (epic livespec-cf4bcu) slice 5: the single
  # all-green gate job. It `needs:` every check-bearing job and fails if any
  # dependency failed or was cancelled — a pure GitHub-expression gate (no
  # external action, no direct tool invocation, so it satisfies
  # non-functional-requirements.md §"Toolchain pins"). Branch protection
  # requires ONLY this context, so one stable name gates the whole set of
  # matrices + dedicated jobs and never churns as checks are added. A SKIPPED
  # dependency (e.g. the entire check-python or e2e-cli job on a zero-.py PR)
  # is not a failure, so ci-green stays green then. export-telemetry is
  # push/merge_group-only and NOT a gate, so it is intentionally absent from
  # `needs:`.
  ci-green:
    name: ci-green
    needs: [check-python, check-doctor-static, check-metadata, e2e-cli, acceptance]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: All required checks passed
        if: ${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}
        run: exit 1
