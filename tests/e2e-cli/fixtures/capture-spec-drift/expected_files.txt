# No project-root file assertion: capture-spec-drift hands each finding off
# to the UPSTREAM livespec plugin's /livespec:propose-change, which writes
# into the spec tree's proposed_changes/ — an external spec target, not an
# impl-git-jsonl project-root artifact. Success is the exit-0 round-trip;
# the absent file list means no per-skill file assertion (contract req 4).
